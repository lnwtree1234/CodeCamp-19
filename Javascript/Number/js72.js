let num = 255;
let binary = num.toString(2);  // แปลงเป็นเลขฐาน 2
console.log(binary);  // ผลลัพธ์: 11111111

let octal = num.toString(8);  // แปลงเป็นเลขฐาน 8
console.log(octal);  // ผลลัพธ์: 377

let hex = num.toString(16);  // แปลงเป็นเลขฐาน 16
console.log(hex);  // ผลลัพธ์: ff
