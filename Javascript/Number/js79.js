function diagonal(width, height) {
    return Math.sqrt((width * width) + (height * height));
}
console.log(diagonal(3, 4));  // ผลลัพธ์: 5 (ความยาวเส้นทแยงมุมของสี่เหลี่ยมผืนผ้าที่มีความกว้าง 3 และสูง 4)
