// code ทั้งสองอันนี้จะแสดง alert ออกมาเหมือนกันทั้งหมดหรือไม่
// ตอบ ไม่เหมือนกันทั้งหมด

// โค้ดแรก:
// let i = 0;
// while (++i < 5) {
//   alert(i);
// }

// โค้ดนี้ใช้ ++i ซึ่งเป็น pre-increment หมายความว่า i จะถูกเพิ่มขึ้นก่อนนำค่าไปใช้ในเงื่อนไข และใน alert()

// โดย แต่ละลูปจะเพิ่มค่า i ก่อน จากนั้นจะเช็คเงื่อนไข i < 5 และแสดง i หลังจากเพิ่มค่าแล้ว
// ค่าที่แสดงใน alert จะเป็น: 1, 2, 3, 4

// โค้ดที่สอง:
// let i = 0;
// while (i++ < 5) {
//   alert(i);
// }

// โค้ดนี้ใช้ i++ ซึ่งเป็น post-increment หมายความว่า i จะถูกใช้งานในเงื่อนไขก่อน และจะเพิ่มค่า i หลังจากเช็คเงื่อนไขและแสดง i
// แต่ละลูปจะเช็คเงื่อนไขโดยใช้ค่าปัจจุบันของ i (ก่อนเพิ่มค่า) แล้วแสดงผล i ที่ถูกเพิ่มค่าหลังจากนั้น
// ค่าที่แสดงใน alert จะเป็น: 1, 2, 3, 4, 5