// โค้ดทั้งสองนี้จะแสดงผล เหมือนกันทั้งหมด เพราะการใช้ i++ (post-increment) และ ++i (pre-increment) 
// ในเงื่อนไขของลูป for ในกรณีนี้ให้ผลลัพธ์ที่เหมือนกัน
// เนื่องจากการเพิ่มค่า i จะเกิดขึ้นหลังจากที่ทำงานในแต่ละรอบของลูปเสร็จสิ้น